# Welcome to Re-Rent-Al app 👋

## Getting Started Setup

```
1. Redux & RTKQuery.  //npx expo install react-redux and npm i @reduxjs/toolkit for state management and api call. -
2. NativeWind. // reactnativereusable library follow documentation. // for design // follow official documentation. -
3. Formik or React-hook-form and yup // npm install formik yup for typescript if use then with npm install -D @types/yup  // for validation -
4. react-native-reanimated-carousel // npx expo install react-native-reanimated-carousel  for carousel. -
5. react-native-reanimated  //  npx expo install react-native-reanimated & dependency for better npm install react-native-worklets  for animation. -
6. react-native-element-dropdown  // npm i react-native-element-dropdown --save for select. -
7. expo-image-picker  // npx expo install expo-image-picker  for image pic from device. -
8. expo-document-picker  // npx expo install expo-document-picker  for image pic from device. -
9. expo-location  // npx expo install expo-location  for location. -
10. expo-permissions  // npx expo install expo-permissions  for location permission.
11. react-native-calendars  // npm i react-native-calendars for date pick
12. expo-secure-store  // npx expo install expo-secure-store  for secure store. -
13. @react-native-async-storage/async-storage  // npm install @react-native-async-storage/async-storage for other store value. -
14. dotenv // npm install dotenv for environment. -
15. npx expo install react-native-svg lucide-react-native  // icons for use.
```

